// 将会被自动应用的全局样式文件，会生成在最终的 CSS 文件结尾，具有比默认样式更高的优先级。

.theorem
  margin 1rem 0
  padding .1rem 1.5rem
  border-radius 0.4rem
  background-color #f0f4f8
  .title
    font-weight bold

.custom-block
  &.right
    color transparentify($textColor, 0.4)
    font-size 0.9rem
    text-align right


.theme-default-content p, ul, ol {
    font-size: small;
    line-height: 1.6
}

.theme-default-content pre[class*="language-"]{
    font-size: small
    line-height: 1.4
    border-radius: 10px;
    padding: 0.5rem 0.75rem;
}

.theme-default-content code {
  font-family: Menlo, Console, Manaco
  color: #f5ab00
  background-color: rgba(151, 180, 210, 0.05)
}

blockquote{
  border-left: 4px solid #49ac8348
  background-color: rgba(66, 185, 131, 0.062)
}

table {
  font-size: small;
}